<UserControl x:Class="LLMClient.UI.Dialog.ResponseViewItemEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:LLMClient.UI.Dialog"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance local:EditableResponseViewItem}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ScrollViewer>
            <ItemsControl ItemsSource="{Binding TextContents,Mode=OneTime}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type local:EditableTextContent}">
                        <Grid>
                            <TextBox Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     Margin="5" />
                            <Button Content="{materialDesign:PackIcon Kind=Restore}"
                                    Command="{Binding RecoverCommand}"
                                    VerticalAlignment="Top"
                                    HorizontalAlignment="Right"
                                    Margin="5"
                                    Style="{StaticResource MaterialDesignFloatingActionMiniButton}" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <Button Grid.Row="1"
                Content="保存"
                HorizontalAlignment="Right"
                Margin="5"
                Command="{Binding  SaveCommand}" />
    </Grid>
</UserControl>