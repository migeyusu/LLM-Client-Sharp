<Window x:Class="LLMClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LLMClient"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Style="{StaticResource MaterialDesignWindow}"
        Title="MainWindow" Height="850" Width="1600"
        d:DataContext="{d:DesignInstance local:MainWindow}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <ListBox SelectedItem="{Binding PreDialog,Mode=OneWayToSource}"
                 ItemsSource="{Binding DialogViewModels,Mode=OneWay}">
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type local:DialogView}">
                    
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        
        <local:DialogView Grid.Row="0"
                          Grid.Column="1" VerticalAlignment="Stretch"
                          HorizontalAlignment="Stretch"
                          DataContext="{Binding PreDialog,Mode=OneWay}" />
    </Grid>
</Window>