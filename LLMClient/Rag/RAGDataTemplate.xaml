<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:rag="clr-namespace:LLMClient.Rag"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
                    xmlns:converters="clr-namespace:LLMClient.UI.Component.Converters">
    <HierarchicalDataTemplate x:Key="ChunkNodeDataTemplate"
                              DataType="{x:Type rag:ChunkNode}"
                              ItemsSource="{Binding Children,Mode=OneTime}">
        <Grid DataContext="{Binding Chunk,Mode=OneTime}"
              MaxHeight="200">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="160" />
                <ColumnDefinition Width="500" />
                <ColumnDefinition Width="500" />
                <ColumnDefinition Width="300" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding Title,Mode=OneTime}"
                       Grid.RowSpan="2"
                       TextWrapping="Wrap"
                       FontWeight="Bold"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Top"
                       Margin="0,0,0,0" />
            <TextBlock Grid.Row="0" Grid.Column="1"
                       Text="Summary"
                       HorizontalAlignment="Center"
                       FontWeight="Black"
                       Visibility="{Binding Summary,Mode=OneTime,Converter={x:Static converters:SnapConverters.StringToVisibilityConverter}}" />
            <TextBox Grid.Row="1" Grid.Column="1"
                     Text="{Binding Summary,Mode=OneTime}"
                     TextWrapping="Wrap"
                     IsReadOnly="True"
                     TextAlignment="Left"
                     materialDesign:TextFieldAssist.DecorationVisibility="Collapsed"
                     BorderThickness="0" />
            <TextBlock Grid.Row="0" Grid.Column="2"
                       Text="Text"
                       HorizontalAlignment="Center"
                       FontWeight="Black"
                       Visibility="{Binding Text,Mode=OneTime,Converter={x:Static converters:SnapConverters.StringToVisibilityConverter}}" />
            <TextBox Grid.Row="1" Grid.Column="2"
                     Text="{Binding Text,Mode=OneTime}"
                     TextWrapping="Wrap"
                     IsReadOnly="True"
                     TextAlignment="Left"
                     materialDesign:TextFieldAssist.DecorationVisibility="Collapsed"
                     BorderThickness="0" />
            <TextBlock Grid.Row="0" Grid.Column="3"
                       Text="Image"
                       HorizontalAlignment="Center"
                       FontWeight="Black"
                       Visibility="{Binding Attachment,Mode=OneTime,Converter={x:Static converters:SnapConverters.StringToVisibilityConverter}}" />
            <ItemsControl Grid.Row="1" Grid.Column="3"
                          ItemsSource="{Binding AttachmentImages,Mode=OneTime}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type ImageSource}">
                        <Image Source="{Binding}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Separator Grid.Row="1"
                       Grid.Column="0"
                       Grid.ColumnSpan="3"
                       Margin="0"
                       Height="1"
                       VerticalAlignment="Bottom"
                       Style="{StaticResource MaterialDesignDarkSeparator}" />
        </Grid>
    </HierarchicalDataTemplate>
</ResourceDictionary>