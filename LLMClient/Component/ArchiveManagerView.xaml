<UserControl x:Class="LLMClient.Component.ArchiveManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewModel="clr-namespace:LLMClient.Component.ViewModel"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance viewModel:ArchiveManagerViewModel}"
             Height="600" Width="900">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                    Command="{Binding RefreshCommand}"
                    Content="刷新"
                    Margin="0,0,10,0" />

            <Button Style="{StaticResource MaterialDesignRaisedButton}"
                    Command="{Binding RestoreCommand}"
                    IsEnabled="{Binding SelectedArchive,Mode=OneWay,Converter={x:Static materialDesign:NullableToVisibilityConverter.CollapsedInstance}}"
                    Content="还原选中存档"
                    Margin="0,0,10,0" />

            <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                    Command="{Binding DeleteCommand}"
                    IsEnabled="{Binding SelectedArchive,Mode=OneWay,Converter={x:Static materialDesign:NullableToVisibilityConverter.CollapsedInstance}}"
                    Content="删除"
                    Foreground="Red"
                    BorderBrush="Red"
                    Margin="0,0,10,0" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" MinWidth="200" MaxWidth="400" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TabControl Grid.Column="0" Style="{StaticResource MaterialDesignTabControl}"
                        materialDesign:TabAssist.HeaderBehavior="Wrapping"
                        materialDesign:DialogHost.RestoreFocusElement="{x:Null}">
                <TabItem Header="Dialogs">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <ListBox Grid.Row="1" ItemsSource="{Binding DialogArchives}"
                                 SelectedItem="{Binding SelectedArchive}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="2">
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" />
                                        <TextBlock Text="{Binding Date}" FontSize="12" Foreground="Gray" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </TabItem>
                <TabItem Header="Projects">
                    <Grid>
                        <ListBox ItemsSource="{Binding ProjectArchives}" SelectedItem="{Binding SelectedArchive}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="2">
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" />
                                        <TextBlock Text="{Binding Date}" FontSize="12" Foreground="Gray" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </TabItem>
            </TabControl>

            <materialDesign:Card Grid.Column="1" Margin="16,0,0,0" Padding="8" UniformCornerRadius="8">
                <ScrollViewer>
                    <TextBox Text="{Binding PreviewContent, Mode=OneWay}"
                             IsReadOnly="True"
                             TextWrapping="Wrap"
                             FontFamily="Consolas"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             BorderThickness="0" />
                </ScrollViewer>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>