<UserControl x:Class="LLMClient.Endpoints.EndpointInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:endpoints="clr-namespace:LLMClient.Endpoints"
             xmlns:abstraction="clr-namespace:LLMClient.Abstraction"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance endpoints:EndpointInfoViewModel}"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" MinWidth="180" MaxWidth="400" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <!-- Left: Endpoint list -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="终结点列表"
                       Style="{StaticResource MaterialDesignSubtitle2TextBlock}"
                       Margin="12,10,12,6"
                       Foreground="{DynamicResource MaterialDesignBodyLight}" />

            <ListBox Grid.Row="1"
                     ItemsSource="{Binding Endpoints, Mode=OneWay}"
                     SelectedItem="{Binding SelectedEndpoint, Mode=TwoWay}"
                     BorderThickness="0"
                     Margin="0,0,0,0">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem"
                           BasedOn="{StaticResource MaterialDesignListBoxItem}">
                        <Setter Property="Padding" Value="12,10" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type abstraction:ILLMAPIEndpoint}">
                        <StackPanel Orientation="Horizontal">
                            <Image Width="24" Height="24"
                                   RenderOptions.BitmapScalingMode="HighQuality"
                                   VerticalAlignment="Center"
                                   Source="{Binding Icon.CurrentSource, Mode=OneWay}" />
                            <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding DisplayName, Mode=OneWay}"
                                           Style="{StaticResource MaterialDesignSubtitle2TextBlock}"
                                           TextTrimming="CharacterEllipsis"
                                           MaxWidth="180" />
                                <TextBlock Text="{Binding Name, Mode=OneWay}"
                                           Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                           Foreground="{DynamicResource MaterialDesignBodyLight}"
                                           TextTrimming="CharacterEllipsis"
                                           MaxWidth="180" />
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

        <!-- Splitter -->
        <GridSplitter Grid.Column="1"
                      Width="3"
                      VerticalAlignment="Stretch"
                      HorizontalAlignment="Center"
                      Style="{StaticResource MaterialDesignGridSplitter}"
                      Margin="0,8" />

        <!-- Right: Content area (留空等候下一步设计) -->
        <Border Grid.Column="2" Margin="16">
            <Grid>
                <!-- Shown when nothing is selected -->
                <TextBlock HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Text="请从左侧选择一个终结点"
                           Foreground="{DynamicResource MaterialDesignBodyLight}"
                           FontSize="14">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedEndpoint}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- Content placeholder shown when endpoint is selected (待设计) -->
                <Border Visibility="{Binding SelectedEndpoint, Converter={x:Static materialDesign:NullableToVisibilityConverter.CollapsedInstance}}">
                    <TextBlock Text="内容区域（待设计）"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                               Foreground="{DynamicResource MaterialDesignBodyLight}" />
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>

