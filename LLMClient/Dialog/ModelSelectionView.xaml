<UserControl x:Class="LLMClient.Dialog.ModelSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dialog="clr-namespace:LLMClient.Dialog"
             xmlns:viewModel="clr-namespace:LLMClient.Component.ViewModel"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance dialog:ModelSelectionPopupViewModel}"
             d:DesignHeight="400" d:DesignWidth="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Button Grid.Row="0"
                HorizontalAlignment="Right"
                Style="{StaticResource PopupCloseButtonStyle}" />
        <ContentPresenter Grid.Row="1"
                          Content="{Binding}"
                          ContentTemplate="{StaticResource FullModelSelectionDataTemplate}" />
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Style="{StaticResource MaterialDesignSubtitle2TextBlock}"
                       VerticalAlignment="Center"
                       Margin="10,0">
                <Run
                    Text="{Binding SelectedModel.Endpoint.DisplayName, StringFormat=选择的服务：{0},TargetNullValue=未选择服务,Mode=OneWay}" />
                <Run Text="{Binding SelectedModel.Name,StringFormat=选择的模型：{0},TargetNullValue=未选择模型,Mode=OneWay}" />
            </TextBlock>
            <Button Grid.Column="1"
                    Content="确认"
                    Width="90"
                    Command="{x:Static viewModel:BaseModelSelectionViewModel.CreateDefaultCommand}"
                    CommandParameter="{Binding}" />
        </Grid>
    </Grid>
</UserControl>