<Window x:Class="LLMClient.Dialog.MultiResponseCompareWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:LLMClient.Dialog"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance local:MultiResponseCompareWindow}"
        Title="Compare All Response" Height="650" Width="900">
    <Grid>
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding ResponseViewItems,Mode=OneTime}"
                          ItemsPanel="{StaticResource HorizontalItemsPanelTemplate}"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto">
                <ItemsControl.CommandBindings>
                    <CommandBinding Command="Delete"
                                    Executed="DeleteItemCommandBinding_OnExecuted" />
                </ItemsControl.CommandBindings>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type local:ComparableResponseViewItem}">
                        <Border BorderBrush="Gray" BorderThickness="1" Margin="4"
                                Width="{Binding Width}" MinWidth="200">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition />
                                    <ColumnDefinition Width="8" />
                                </Grid.ColumnDefinitions>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Image Source="{Binding  RawItem.Icon.CurrentSource,Mode=OneWay}"
                                           Margin="5"
                                           Width="35"
                                           Height="35" />
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding RawItem.ModelName,Mode=OneTime}"
                                               Style="{StaticResource MaterialDesignSubtitle2TextBlock}"
                                               VerticalAlignment="Center"
                                               HorizontalAlignment="Center"
                                               TextWrapping="Wrap"
                                               TextTrimming="CharacterEllipsis" />
                                    <Button Grid.Column="2"
                                            Width="20"
                                            Height="20"
                                            ToolTip="移除对比"
                                            Command="Delete"
                                            CommandParameter="{Binding}"
                                            Template="{StaticResource ItemDeleteButtonTemplate}"
                                            Margin="5" />
                                </Grid>
                                <FlowDocumentScrollViewer Grid.Row="1"
                                                          Grid.Column="0"
                                                          ScrollViewer.PanningMode="Both"
                                                          ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                                          Document="{Binding  FullDocument,Mode=OneWay}" />
                                <Thumb Grid.Row="0"
                                       Grid.RowSpan="2"
                                       Grid.Column="1" Cursor="SizeWE"
                                       DragDelta="Thumb_DragDelta"
                                       Background="Transparent" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>