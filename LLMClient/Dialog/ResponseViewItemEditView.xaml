<UserControl x:Class="LLMClient.Dialog.ResponseViewItemEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:LLMClient.Dialog"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance local:DialogItemEditViewModel}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ScrollViewer>
            <ItemsControl ItemsSource="{Binding TextContents,Mode=OneTime}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type local:EditableTextContent}">
                        <Grid>
                            <TextBox Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     AcceptsReturn="True"
                                     VerticalScrollBarVisibility="Auto"
                                     AcceptsTab="True"
                                     TextWrapping="Wrap"
                                     Margin="5" />
                            <Button Content="{materialDesign:PackIcon Kind=Undo}"
                                    Command="{Binding RecoverCommand}"
                                    Visibility="{Binding HasEdit,Mode=OneWay,Converter={StaticResource BooleanToVisibilityConverter}}"
                                    VerticalAlignment="Top"
                                    HorizontalAlignment="Right"
                                    Margin="5"
                                    Width="30"
                                    Height="30"
                                    ToolTip="Revert to original string"
                                    Style="{StaticResource MaterialDesignFloatingActionMiniButton}" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <Button Grid.Row="1"
                Content="确认更改"
                HorizontalAlignment="Right"
                Margin="5"
                Command="{Binding  SaveCommand}" />
    </Grid>
</UserControl>