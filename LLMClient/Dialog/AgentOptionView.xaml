<UserControl x:Class="LLMClient.Dialog.AgentOptionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:LLMClient.Dialog"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:extension="clr-namespace:LLMClient.Component.Extension"
             xmlns:miniSwe="clr-namespace:LLMClient.Agent.MiniSWE"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance local:AgentOption}"
             d:DesignHeight="300" d:DesignWidth="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            
            <ComboBox Grid.Column="0"
                      Margin="0,0,10,0"
                      materialDesign:HintAssist.Hint="Platform"
                      ItemsSource="{Binding Source={extension:EnumBindingSource {x:Type miniSwe:RunPlatform}}}"
                      SelectedItem="{Binding Platform,Mode=TwoWay}"
                      MinWidth="120"/>

            <Grid Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0"
                         TextWrapping="NoWrap"
                         Margin="5,0"
                         materialDesign:HintAssist.Hint="WorkingDirectory"
                         Text="{Binding WorkingDirectory,Mode=TwoWay}"
                         TextAlignment="Left" />
                <Button Grid.Column="1"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Margin="0,0,5,0"
                        Width="30"
                        Height="30"
                        Click="SelectFolder_OnClick"
                        Content="{materialDesign:PackIcon Kind=FolderSettingsOutline,Size=16}"
                        Style="{StaticResource MaterialDesignIconForegroundButton}" />
            </Grid>
        </Grid>
    </Grid>
</UserControl>
